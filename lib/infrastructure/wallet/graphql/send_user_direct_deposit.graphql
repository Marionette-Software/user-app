mutation sendUserDirectDeposit(
  $currencyPaymentInterfaceId: String!
  $address: String!
  $amount: Float!
) {
  userDirectDeposit(
    currencyPaymentInterfaceId: $currencyPaymentInterfaceId
    address: $address
    amount: $amount
  )
}
