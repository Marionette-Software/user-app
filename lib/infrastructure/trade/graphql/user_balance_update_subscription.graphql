subscription GetUserBalanceUpdated($token: String!) {
  userBalanceUpdated(token: $token) {
    currencyId
    balance
    advancedTradingLockedBalance
    advancedTradingBalance
    ts
    lockedBalance
  }
}
